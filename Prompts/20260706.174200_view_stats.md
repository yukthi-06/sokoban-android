> No. when I view a solved game, just show me the stats. I am not playing that game currently, so current moves is not required.
> Just use the fields used for unsolved game to show the solved game stats

# Changes Made
- Reverted the inline stat changes.
- Updated `GameActivity.java` so that if the game hasn't started yet (`movesCount == 0`) and it has a solution, the fields display only the best stats.
- Upon making a move, the fields instantly transition back to displaying the active game stats.
