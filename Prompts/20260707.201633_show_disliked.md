# User Request
> rename 'Hide disliked levels in browsing' to 'Show disliked levels' and flipits meaning in application

# Implementation Details

To align the disliked levels filter with the new "Show completed levels" filter, the following changes were made:

1. **Settings Screen Updates (`activity_settings.xml`)**:
   - Renamed the checkbox text from "Hide disliked levels in browsing" to "Show disliked levels".
   - Wrapped the checkbox in a `LinearLayout` to match the exact visual style of the "Show completed levels" checkbox (with the checkbox element placed perfectly on the right side of the text).

2. **Preference Keys & Logic (`SettingsActivity.java`, `MainActivity.java`, `GameActivity.java`)**:
   - Renamed the SharedPreferences key from `hide_disliked` to `show_disliked`.
   - Updated variables across all files from `hideDisliked` to `showDisliked`.
   - Inverted the default value: it now defaults to `true` (meaning disliked levels are shown by default).
   - Inverted the filtering logic: `MainActivity` and `GameActivity` (when using left/right arrows) now check if `!showDisliked` is true before skipping a level.
   - The JSON file saving/loading was also updated to read and write `show_disliked`.
