> in the game level screen, along with Moves and Pushes, also show running time taken.   
> The same layout can be used to show the solved level details

# Changes Made
- Added a `timeText` field to `activity_game.xml` alongside Moves and Pushes.
- Initially added a separate layout row for the best stats.
- Added a 1-second interval timer in `GameActivity` using `Handler` to track the active running time.
