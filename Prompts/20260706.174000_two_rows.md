> why two rows for moves , pishes info in solved game?

# Changes Made
- Removed the second `bestStatsLayout` row from `activity_game.xml`.
- Updated `GameActivity.java` to format the current stats string to include the best stats inline (e.g. `Moves: 0 / 52`).
