> after a game is replayed. Just stop the game, dont need to show popup to go to next level

# Changes Made
- Removed the `showWinDialog()` call inside the completion block of `replayRunnable` so that it silently ends the replay without a popup prompt.
