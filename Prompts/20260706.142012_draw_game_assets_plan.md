Prompt: to draw the game, use assets from 'X:\Projects_X\0_Active\8_Android_APK\04_Games\00-My-Own\Sokoban_APK_GITHUB\assets'.  They have been named appropriately

# Draw Game with Bitmap Assets

The goal is to update the game drawing logic to use bitmap assets instead of procedural geometric shapes (circles, rectangles).

## User Review Required

Because of a known system limitation preventing me from copying binary files programmatically in this environment, I need your help to copy and rename the assets into the Android resources folder before I can proceed with the code changes. 

Please perform the following steps:
1. Create a directory named `drawable` inside `sokoban-android\src\main\res\` if it doesn't already exist.
2. Copy the files from `assets` into `sokoban-android\src\main\res\drawable` and rename them as follows (Android requires drawable names to be lowercase and contain only a-z, 0-9, and underscores):
   - `0-path.png` -> `path.png`
   - `1-wall.png` -> `wall.png`
   - `2-pusher.png` -> `pusher.png`
   - `3-box.png` -> `box.png`
   - `4-destination-unfilled.png` -> `destination_unfilled.png`
   - `5-box-in-destination.png` -> `box_in_destination.png`
   - `6-pusher.png` -> `pusher_on_goal.png`
   - `7-blank.png` -> `blank.png`

## Open Questions

- Does the game logic differentiate between a `pusher` on a regular floor versus a `pusher` on a destination? If not, we can just use `pusher.png` for both. I plan to use `pusher_on_goal.png` (originally `6-pusher.png`) for `PLAYER_ON_GOAL`. Let me know if that is incorrect.

## Proposed Changes

### sokoban-android\src\main\java\com\sokoban\android\view\GameView.java

#### [MODIFY] GameView.java
- Load `Bitmap` objects in the constructor or `init()` using `BitmapFactory.decodeResource(getResources(), R.drawable.xxx)`.
- Update `onDraw()` to use `canvas.drawBitmap(bitmap, null, destRect, paint)` instead of `drawRect()` and `drawCircle()`.
- The following mappings will be implemented:
  - `WALL` -> `wall.png`
  - `BOX` -> `box.png`
  - `GOAL` -> `destination_unfilled.png`
  - `BOX_ON_GOAL` -> `box_in_destination.png`
  - `PLAYER` -> `pusher.png`
  - `PLAYER_ON_GOAL` -> `pusher_on_goal.png`
  - `Floor/Empty` -> `path.png`

## Verification Plan

- After modifying the code, I will ask you to build and run the app to ensure that the graphical assets display correctly.
